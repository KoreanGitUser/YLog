import { useEffect } from "react";
import UnAuthRoutes from "@components/routes/UnAuthRoutes";

const App = () => {
  useEffect(() => {}, []);

  return (
    <div>
      <UnAuthRoutes />
    </div>
  );
};

export default App;
